PROJECT.md — คำสั่งทำงานสำหรับ Dev/AI Agent

เป้าหมายเอกสารนี้: ให้ dev คนไหนก็ตาม (หรือ AI agent ไหนก็ตาม) หยิบงานต่อจากเครื่องไหนก็ได้ โดยรู้ว่าตอนนี้ทำถึงไหน และต้อง backup/ปิดเฟสอย่างไรก่อนเริ่มเฟสถัดไป

กติกาหลัก (ต้องทำทุกเฟส):

ก่อนเริ่มเฟส — อ่านไฟล์ PROGRESS.md เพื่อดูว่าเฟสก่อนหน้าปิดสถานะว่าอย่างไร
ระหว่างทำเฟส — commit code ลง Git บ่อยๆ (ทุกงานย่อยที่เสร็จ) พร้อม message บอกว่าทำอะไร
ก่อนปิดเฟส (บังคับ):
รัน test/เช็คว่าฟีเจอร์ของเฟสนั้นทำงานจริง (ไม่ใช่แค่ compile ผ่าน)
Push code ขึ้น Git remote (GitHub) — ห้ามเก็บไว้ในเครื่องอย่างเดียว
Backup ฐานข้อมูล/ค่า config ที่เปลี่ยน (ถ้ามี) แยกไว้เป็นไฟล์ต่างหาก
อัปเดต PROGRESS.md เขียนสรุป: เฟสนี้ทำอะไรเสร็จ, มีอะไรค้าง, ไฟล์ไหนถูกแก้บ้าง
เปิดเฟสใหม่ — ต้องดึง (pull) code ล่าสุดจาก Git ก่อนเริ่ม ไม่ทำงานต่อจากไฟล์เก่าในเครื่อง

โครงสร้างไฟล์ที่ต้องมีในโปรเจกต์:

/PROJECT.md       ← เอกสารนี้ (กติกาการทำงาน)
/PROGRESS.md      ← สถานะปัจจุบัน (อัปเดตทุกจบเฟส)
/backups/         ← เก็บ backup DB/config แยกตามวันที่-เฟส

ตัวอย่างเนื้อหาใน PROGRESS.md ที่ต้องอัปเดตทุกครั้ง:

เฟสที่ทำล่าสุด: เฟส 2 - Game Logic
สถานะ: ปิดเฟสแล้ว (2026-XX-XX)
สิ่งที่เสร็จ: ตรวจกติกาไพ่ครบทุกแบบ, ระบบบอทเดินแทนพื้นฐาน
สิ่งที่ยังไม่เสร็จ/รู้ว่ามีบั๊ก: บอทยังเลือกไพ่แบบสุ่ม ยังไม่ฉลาด
ไฟล์หลักที่แก้: /backend/game-logic.ts, /backend/bot.ts
เฟสถัดไปต้องเริ่มจาก: เฟส 3 - Realtime Sync
