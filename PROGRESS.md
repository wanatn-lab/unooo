เฟสที่ทำล่าสุด: เฟส 1 - Room System
สถานะ: ปิดเฟสแล้ว (2026-09-25)

สิ่งที่เสร็จ:
- ระบบสร้างห้อง (Create Room) — สุ่มรหัสห้อง 6 หลัก ไม่ซ้ำกัน (การันตีด้วย unique
  constraint + retry ในฝั่ง database function ไม่ใช่แค่สุ่มฝั่ง client)
- ระบบเข้าร่วมห้องผ่านลิงก์/รหัสห้อง (Join Room) — เช็คห้องมีจริงไหม, เต็มไหม
  (จำกัด 8 คน) ก่อนให้เข้า ถ้าไม่ผ่านจะขึ้นข้อความ error ชัดเจน ไม่ใช่หน้าเปล่า/ค้าง
- Lobby แบบ realtime — ทุกเครื่องที่อยู่ในห้องเดียวกันเห็นชื่อผู้เล่นที่เข้าร่วมใหม่
  ทันทีโดยไม่ต้อง refresh (ผ่าน Supabase Realtime)
- หน้า UI (Home / Lobby / Error) — ปรับธีมเป็นธีม "404: Afterparty Not Found"
  (โทนมืด นีออน, ภาพพื้นหลังคลับเคลื่อนไหวเบาๆ, การ์ดกระจกฝ้า, avatar 8 แบบ
  ประดับในรายชื่อผู้เล่น) ตามไฟล์ตัวอย่างที่ลูกค้าส่งมาและอนุมัติให้ใส่ animation
  ได้ (แก้จากแผนเดิมของเฟสนี้ที่ตั้งใจทำ static — ส่วนนี้เป็นการตัดสินใจของลูกค้า
  ไม่ใช่ scope creep ที่ผมเพิ่มเอง) มีปุ่ม "Reduce motion" ให้ปิด animation ทั้งหมด
  ได้เสมอ (จำค่าไว้ต่อเบราว์เซอร์) และเคารพ prefers-reduced-motion ของระบบด้วย —
  ฟังก์ชันการทำงานจริง (สร้าง/เข้าห้อง/realtime) ไม่เปลี่ยนแปลง
- Backend: Supabase project ใหม่แยกเฉพาะเกม UNO (ไม่ปนกับ project เดิม), ตาราง
  rooms/players, RPC function create_room()/join_room() ที่ทำงานแบบ atomic
  (กันปัญหา race condition ตอนสองคนสร้าง/เข้าห้องพร้อมกัน), เปิด Row Level
  Security + Realtime แล้ว

การทดสอบที่ทำจริง (ไม่ใช่แค่ compile ผ่าน):
- สร้างห้อง 5 ห้องติดกัน → รหัสไม่ซ้ำกันเลย (ยืนยันด้วย SQL โดยตรงกับฐานข้อมูลจริง)
- จำลอง 4 คนเข้าห้องเดียวกัน (host + 3 คน join) → ทุกคนถูกบันทึกในห้องถูกต้อง
- เติมห้องจนครบ 8 คน แล้วให้คนที่ 9 join → ถูกปฏิเสธด้วย error "room_full" ตามที่
  ออกแบบไว้
- ลอง join ด้วยรหัสห้องที่ไม่มีอยู่จริง → ถูกปฏิเสธด้วย error "room_not_found"
- ตรวจโค้ด frontend ทั้งหมดผ่าน syntax check + รันในเบราว์เซอร์จริง (Playwright)
  ยืนยันว่าหน้า Home โหลดขึ้น ปุ่มกดได้ ไม่มี JS error
- ถ่ายภาพหน้าจอ Home (มือถือ/desktop) และ Lobby (จำลองรายชื่อผู้เล่น) ยืนยันว่า
  ธีมใหม่ขึ้นถูกต้อง, ทดสอบปุ่ม "Reduce motion" ว่ากดแล้ว toggle จริงและจำค่าไว้

ข้อจำกัดของการทดสอบรอบนี้ (สำคัญ ต้องอ่าน):
- Sandbox ที่ใช้รันงานนี้ (คลาวด์) มีการปิดกั้นเครือข่ายขาออกไปยังโดเมนทั่วไป
  (เช่น supabase.co) ตามนโยบายองค์กร จึงไม่สามารถเปิดเบราว์เซอร์หลายตัวพร้อมกัน
  แล้วยิง network จริงไปที่ Supabase จากในนี้ได้ ผมจึงทดสอบ "ตรรกะฝั่ง database"
  ทั้งหมดโดยตรงผ่าน SQL กับฐานข้อมูลจริง (ผลตรงกับที่ frontend เรียกใช้ทุกจุด)
  แต่ยังไม่ได้ทดสอบ "เปิดลิงก์เดียวกันจาก 2-3 เบราว์เซอร์จริงพร้อมกัน" แบบเห็นผล
  บนหน้าจอด้วยตาตัวเอง — แนะนำให้ทดสอบขั้นตอนนี้อีกครั้งหลัง deploy จริง (ขั้นตอน
  อยู่ใน README.md หัวข้อ "Testing the room flow" ใช้เวลาไม่ถึง 5 นาที)

สิ่งที่ยังไม่เสร็จ/รู้ว่ามีบั๊ก (ตั้งใจเลื่อนไปเฟสหลัง ไม่ใช่ลืมทำ):
- ยังไม่มี animation ของ "เกม" (แจกไพ่/เล่นไพ่/flip การ์ด ฯลฯ) — ที่ทำรอบนี้เป็น
  animation ของ UI/lobby เท่านั้น เกมยังไม่เริ่มสร้าง (เฟสถัดไปที่เกี่ยวข้อง:
  เฟส Animation เดิม + เฟส 2 Game Logic)
- ยังไม่มีกติกาไพ่/บอท (เฟส 2 - Game Logic)
- ยังไม่มี realtime sync ของ "การเล่นไพ่ระหว่างเกม" (เฟส 3 - Realtime Sync) —
  เฟสนี้ทำแค่ realtime ของ "รายชื่อผู้เล่นใน lobby" เท่านั้น
- ยังไม่มีระบบ auth/login ผู้เล่นยังระบุตัวตนด้วยชื่อที่พิมพ์เองเท่านั้น (RLS policy
  ปัจจุบันเปิดกว้างไว้ก่อนตามที่เหมาะกับเฟสนี้ — มีคอมเมนต์เตือนไว้ในไฟล์ migration
  ว่าต้องรัดให้แน่นขึ้นเมื่อมี auth)
- ยังไม่ได้ deploy ขึ้น hosting จริง (Netlify/GitHub Pages ฯลฯ) — โค้ดพร้อม deploy
  แล้ว มีไฟล์ config (_redirects, 404.html) เตรียมไว้ให้แล้วตามที่ README อธิบาย
- ที่มีตัวเลือก "ใช้เด็คย่อ 60 ใบ" ในโปรโตไทป์ UI เดิม (README เก่าที่แนบมา) —
  ไม่เกี่ยวกับเฟสนี้ เป็นเรื่องของเฟส Game Logic ในอนาคต

ไฟล์หลักที่แก้/เพิ่ม:
- /PROJECT.md, /PROGRESS.md (ไฟล์นี้), /README.md
- /frontend/index.html, /frontend/404.html, /frontend/_redirects
- /frontend/css/style.css (ธีมใหม่ทั้งหมด)
- /frontend/js/config.js, supabaseClient.js, router.js, session.js,
  roomApi.js, home.js, lobby.js (เพิ่ม avatar), main.js, motion.js (ใหม่)
- /frontend/vendor/supabase.js (library ที่ดาวน์โหลดมาเก็บไว้ในโปรเจกต์เอง
  แทนที่จะโหลดจาก CDN ตอนรันจริง — กันปัญหา CDN ล่ม/ถูกบล็อก)
- /frontend/assets/afterparty-club.webp, /frontend/assets/avatars/*.webp
  (ภาพจากไฟล์ตัวอย่างที่ลูกค้าส่งมา — ย่อขนาด+แปลงเป็น webp แล้ว จาก 17MB
  เหลือ 472KB รวม เพื่อไม่ให้เว็บโหลดช้าบนมือถือ)
- /backend/README.md, /backend/supabase/migrations/0001_*.sql, 0002_*.sql
- /backups/2026-09-25_phase1/ (backup ของ schema/config ตอนปิดเฟสนี้)

เฟสถัดไปต้องเริ่มจาก: เฟส 2 - Game Logic (ตามที่ PROJECT.md ตัวอย่างระบุไว้)
